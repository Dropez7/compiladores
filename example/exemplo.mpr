func main(){
    int v[][];
    v.append([10, 20]);
    v.append([20, 30, 40]);
    v.append([40, 50, 60, 70]);

    print("Tamanho do vetor:", v.len());
    v.remove(1); // Remove a segunda linha
    print("Tamanho do vetor após remoção:", v.len());
}