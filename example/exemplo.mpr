struct bar {
    int x;
}

func printaVetor(bar vetor[]) {
    print(vetor[0].x)
}

func main() {
    bar v[]

    bar b
    b.x = 42
    v.append(b)

    printaVetor(v) // Saída esperada: 42
}